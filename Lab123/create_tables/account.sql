CREATE TABLE ACCOUNT 
(
  ID NUMBER NOT NULL
, ID_CUSTOMER NUMBER NOT NULL
, DATE_OPEN DATE 
, ID_DEPARTMENT NUMBER NOT NULL
, CONSTRAINT ACCOUNT_PK PRIMARY KEY 
  (
    ID 
  )
  ENABLE 
);

ALTER TABLE ACCOUNT
ADD CONSTRAINT ACCOUNT_CUSTOMER_FK1 FOREIGN KEY
(
  ID_CUSTOMER 
)
REFERENCES CUSTOMER
(
  ID
)
ENABLE;

ALTER TABLE ACCOUNT
ADD CONSTRAINT ACCOUNT_DEPARTMENT_FK1 FOREIGN KEY
(
  ID_DEPARTMENT 
)
REFERENCES DEPARTMENT
(
  ID 
)
ENABLE;
